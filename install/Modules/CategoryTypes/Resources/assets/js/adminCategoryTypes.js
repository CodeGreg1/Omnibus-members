"use strict";

// define app.adminCategoryTypes object
app.adminCategoryTypes = {};


// initialize functions of app.adminCategoryTypes object
app.adminCategoryTypes.init = function() {
    
};

// initialize app.adminCategoryTypes object until the document is loaded
$(document).ready(app.adminCategoryTypes.init());